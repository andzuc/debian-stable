#!/bin/bash
IMAGE="${PWD}/image-vagrant"
mkdir "${IMAGE}"
cd "${BOOTPATH}"
for img in *.img; do
    qemu-img convert -p -f qcow2 -O vdi "${img}" "${IMAGE}/${img%.*}.vdi"
done
cd "${IMAGE}"
md5sum * >.MD5SUMS
mv .MD5SUMS MD5SUMS
cd -

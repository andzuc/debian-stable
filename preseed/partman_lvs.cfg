	300 400 1000000000 ext2
	    $primary{ } $bootable{ }
	    method{ format } format{ }
	    device{ /dev/sda }
	    use_filesystem{ } filesystem{ ext2 }
	    mountpoint{ /boot } .
	1372 1472 -1 ext4
	    $lvmok{ }
	    lv_name{ root }
	    method{ format } format{ }
	    use_filesystem{ } filesystem{ ext4 }
	    mountpoint{ / }
	    in_vg{ rootvg } .
